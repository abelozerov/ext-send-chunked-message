{
  "name": "example",
  "version": "1.0.0",
  "description": "",
  "main": "background.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "esbuild content.js --bundle --outfile=build/content.js && esbuild background.js --bundle --outfile=build/background.js && cp manifest.json build/manifest.json"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "esbuild": "0.21.4"
  },
  "dependencies": {
    "ext-send-chunked-message": "^1.0.0"
  }
}
